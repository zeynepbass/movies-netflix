import React from 'react';

const Hesap = () => {

  // const data = JSON.parse(localStorage.getItem("user"));
  // console.log('Stored User Data:', data);
  return (
    <>
      {/* {data ? (
        <p style={{ color: 'white' }}>{data.result.firstName}</p>
      ) : (
        <p>User data not found</p>
      )} */}
    </>
  );
};

export default Hesap;
