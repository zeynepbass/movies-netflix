import { Feature, FeatureCollection, Geometries } from '@turf/helpers';
import { IRasterLayerData } from '../interface';
interface IDataItem {
    [key: string]: any;
}
export declare function getColumn(data: IDataItem[], columnName: string): number[];
export declare function isRasterFileData(data?: IRasterLayerData): boolean;
export declare function isRasterFileDataArray(data: IRasterLayerData): boolean;
export declare function isNumberArray(data: IRasterLayerData): boolean;
/**
 * enforce polygon ring winding order for geojson
 * https://github.com/mapbox/geojson-rewind
 * @param geojson
 * @returns geojson
 */
export declare function geojsonRewind<T extends FeatureCollection | Feature | Geometries>(geojson: T): T;
export {};
